// GENERATED by Velle from the spec — do not edit. Regenerate with: gradle generate
package velle.generated.enrollment

import velle.generated.EnrollmentSystem

/**
 * The scenarios the generated specs require — the human-owned half of the
 * given/derived split (testgen.md). Implement as:
 *
 *     package velle.generated.enrollment
 *     class Givens(private val sys: EnrollmentSystem) : RequiredGivens { ... }
 *
 * A missing given is a compile error naming exactly what the spec's tests are owed.
 */
interface RequiredGivens {

    /** Commit ONE new 'SetEnrollment' that enters 'SetEnrollment' at its commit. The act is transient — nothing to return. */
    fun setEnrollment()

    /** Commit ONE new 'Enroll' that enters 'Enroll' at its commit. The act is transient — nothing to return. */
    fun enroll()

    /** Commit ONE new 'Drop' that enters 'Drop' at its commit. The act is transient — nothing to return. */
    fun drop()

    /** Commit ONE new 'Tag' that enters 'Tag' at its commit. The act is transient — nothing to return. */
    fun tag()

    /** Commit ONE new 'AssignAdvisor' that enters 'AssignAdvisor' at its commit. The act is transient — nothing to return. */
    fun assignAdvisor()
}
