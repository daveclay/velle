// GENERATED by Velle from the spec — do not edit. Regenerate with: gradle generate
package velle.generated.payments.specs

import kotlin.test.*
import velle.*
import velle.generated.*
import velle.generated.payments.*

/**
 * rule RecordVerdict when ProcessorVerdict {
 *     ChargeResponse from { attempt: attempt, outcome: outcome }
 * }
 *
 * never (ProcessorVerdict where ((outcome != "approved") and (outcome != "declined")) and (outcome != "error"))
 *
 */
class ProcessorVerdictSpec : SpecSupport() {

    @Test
    fun `RecordVerdict - a new ProcessorVerdict produces a ChargeResponse`() {
        val beforeChargeResponse = count("ChargeResponse")
        // given: a 'ProcessorVerdict' committed entering 'ProcessorVerdict' — transient: the act is not kept
        givens.processorVerdict()
        assertEquals(beforeChargeResponse + 1, count("ChargeResponse"), "rule RecordVerdict: one 'ChargeResponse' per firing")
    }
}
