// GENERATED by Velle from the spec — do not edit. Regenerate with: gradle generate
package velle.generated.payments.specs

import kotlin.test.*
import velle.*
import velle.generated.*
import velle.generated.payments.*

/**
 * never (Refund where amount <= 0)
 *
 */
class RefundSpec : SpecSupport() {

    @Test
    fun `never - a Refund where amount at most 0 is refused`() {
        // given: any committed 'Order'
        val order = givens.someOrder()
        val before = count("Refund")
        val result = sys.system.commit("Refund", mapOf("order" to order.id, "amount" to java.math.BigDecimal("-1")))
        assertIs<CommitResult.Refused>(result)
        assertEquals(before, count("Refund"), "a refused act commits nothing")
    }

    @Test
    fun `never - a Refund with amount 1 is accepted`() {
        // given: any committed 'Order'
        val order = givens.someOrder()
        val result = sys.system.commit("Refund", mapOf("order" to order.id, "amount" to java.math.BigDecimal("1")))
        assertIs<CommitResult.Accepted>(result)
    }
}
