# Diagrams — Billing

Generated from the spec — a deterministic projection, regenerated by `gradle generate`; do not edit. Every diagram below is derived from the one authored description: the class diagram shows the shapes and their relationships, the state-flow diagrams show each shape's refinement memberships and what can change them, the rule graph maps the whole system's cause-and-effect on one page, and the sequence diagrams show what one act's arrival may cause. None is maintained by hand, so none can drift from the spec or from each other.

## Class diagram

`/name` marks a derived property — computed, never stored. Solid arrows are declared relationships, labeled with the declaring field (and the inferred inverse collection, where one exists). A hollow-triangle arrow reads "is a refinement of": membership is decided by the predicate in the attached note, instant by instant, not fixed at creation. Dashed arrows connect a composed refinement to the refinements it is built from.

```mermaid
classDiagram
    direction LR
    class Customer {
        <<expose>>
        name: text
        email: text
        signedUpOn: timestamp on create
        largestPayment: decimal = 0
    }
    class CorrectEmail {
        <<expose>>
        corrected: text
    }
    class Invoice {
        <<expose>>
        due: Date
        reference: text = randomUUID
        issuedOn: timestamp on create
        /total: decimal
        /balance: decimal
        /status: text
    }
    class LineItem {
        <<expose>>
        description: text
        price: decimal
        quantity: integer
        /amount: decimal
    }
    class Payment {
        <<expose>>
        amount: decimal
        receivedOn: timestamp on create
    }
    class Receipt {
        sentOn: DateTime
    }
    class ReceiptEmail {
        queuedOn: DateTime
    }
    class Reminder {
        sentOn: Date
    }
    class Issuance {
        <<expose>>
    }
    class ChangeDueDate {
        <<expose transient>>
        newDue: Date
    }
    class DueChangeRefusal {
        requestedDue: Date
        reason: text
        refusedOn: DateTime
    }
    class ArchiveRequest {
        <<expose>>
    }
    class UnarchiveRequest {
        <<expose>>
    }
    class UnarchiveNotice {
        wasArchivedOn: Date
        noticedOn: DateTime
    }
    CorrectEmail "*" --> "1" Customer : customer (inverse correctEmails)
    Invoice "*" --> "1" Customer : customer (inverse invoices)
    LineItem "*" --> "1" Invoice : invoice (inverse lineItems)
    Payment "*" --> "1" Invoice : invoice (inverse payments)
    Receipt "*" --> "1" Invoice : invoice (inverse receipts)
    ReceiptEmail "*" --> "1" Receipt : receipt (inverse receiptEmails)
    Reminder "*" --> "1" Invoice : invoice (inverse reminders)
    Issuance "*" --> "1" Invoice : invoice (inverse issuances)
    ChangeDueDate "*" --> "1" Invoice : invoice
    DueChangeRefusal "*" --> "1" Invoice : invoice (inverse dueChangeRefusals)
    ArchiveRequest "*" --> "1" Invoice : invoice (inverse archiveRequests)
    UnarchiveRequest "*" --> "1" Invoice : invoice (inverse unarchiveRequests)
    UnarchiveNotice "*" --> "1" Invoice : invoice (inverse unarchiveNotices)
    class PaidInvoice {
        <<refinement>>
    }
    Invoice <|-- PaidInvoice
    note for PaidInvoice "= Invoice where (total > 0) and (balance <= 0)"
    class UnemailedReceipt {
        <<refinement>>
    }
    Receipt <|-- UnemailedReceipt
    note for UnemailedReceipt "= Receipt where not (exists ReceiptEmail for this)"
    class OverdueInvoice {
        <<refinement>>
    }
    Invoice <|-- OverdueInvoice
    note for OverdueInvoice "= Invoice where (balance > 0) and (due < today)"
    class ActionableOverdue {
        <<refinement>>
    }
    Invoice <|-- ActionableOverdue
    ActionableOverdue ..> OverdueInvoice
    ActionableOverdue ..> ArchivedInvoice
    note for ActionableOverdue "= OverdueInvoice and not ArchivedInvoice"
    class IssuedInvoice {
        <<refinement>>
        frozen due
    }
    Invoice <|-- IssuedInvoice
    note for IssuedInvoice "= Invoice where exists Issuance for this"
    class ApplicableDueChange {
        <<refinement>>
    }
    ChangeDueDate <|-- ApplicableDueChange
    note for ApplicableDueChange "= ChangeDueDate where not (invoice is IssuedInvoice)"
    class RefusedDueChange {
        <<refinement>>
    }
    ChangeDueDate <|-- RefusedDueChange
    note for RefusedDueChange "= ChangeDueDate where invoice is IssuedInvoice"
    class ArchivedInvoice {
        <<refinement>>
        /archivedOn: Date captured at entry
    }
    Invoice <|-- ArchivedInvoice
    note for ArchivedInvoice "= Invoice where (exists ArchiveRequest for this) and (not (exists UnarchiveRequest for this))"
```

## State flow

A state is membership in a refinement, and memberships overlap — so each diagram below is one *axis* of membership, varying independently of its siblings. Arrow directions are proven from the spec (an `exists` can only be satisfied — v0 never deletes — and a fold moves one way when the input-constrained refusals pin the sign of every contribution); where no proof exists, the edge says **may flip** and is drawn in both directions.

### Invoice

4 refinements form 3 independent membership axes — each Invoice holds a position on every axis at once.
- **ActionableOverdue** = OverdueInvoice and not ArchivedInvoice — a view composed across the axes below, not an axis of its own.
- **RemindOverdue** — runs at every Weekly tick: when ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) — inserts Reminder.

**PaidInvoice / OverdueInvoice** — provably disjoint, one axis. Arrows are drawn through *neither* for legibility; a single commit may move an instance straight between named states.

```mermaid
stateDiagram-v2
    state "neither" as otherwise
    [*] --> otherwise : a new Invoice
    otherwise --> PaidInvoice : commitPayment
    otherwise --> PaidInvoice : commitLineItem — may flip
    PaidInvoice --> otherwise : commitLineItem — may flip
    note right of PaidInvoice : on entry — SendReceipt inserts Receipt (fires in the committing transaction)
    otherwise --> OverdueInvoice : commitLineItem
    otherwise --> OverdueInvoice : time passes
    OverdueInvoice --> otherwise : commitPayment
    otherwise --> OverdueInvoice : ApplyDueChange writes Invoice.due — may flip
    OverdueInvoice --> otherwise : ApplyDueChange writes Invoice.due — may flip
```

**IssuedInvoice** — one-way: entry is permanent.

```mermaid
stateDiagram-v2
    state "not yet IssuedInvoice" as pre
    [*] --> pre : a new Invoice
    pre --> IssuedInvoice : commitIssuance
    note right of IssuedInvoice : one-way — exists Issuance for this can never stop holding (v0 never deletes)
    note right of IssuedInvoice : while a member — due frozen, writes are refused
```

**ArchivedInvoice** — once through: enter once, leave once, never again.

```mermaid
stateDiagram-v2
    state "never a member" as pre
    state "left ArchivedInvoice — permanently" as post
    [*] --> pre : a new Invoice
    pre --> ArchivedInvoice : commitArchiveRequest
    ArchivedInvoice --> post : commitUnarchiveRequest
    note right of post : terminal — exists UnarchiveRequest for this now holds forever (v0 never deletes), so membership can never be regained
    note right of ArchivedInvoice : on entry — captured archivedOn = today, readable even after leaving
    note right of ArchivedInvoice : on exit — NoteUnarchival inserts UnarchiveNotice (fires in the committing transaction)
```

### Receipt

**UnemailedReceipt** — born a member; leaving is permanent.

```mermaid
stateDiagram-v2
    state "no longer UnemailedReceipt" as post
    [*] --> UnemailedReceipt : a new Receipt is born a member — not (exists ReceiptEmail for this) already holds
    UnemailedReceipt --> post : EmailReceipt inserts ReceiptEmail
    note right of post : one-way — exists ReceiptEmail for this now holds forever (v0 never deletes)
    note right of UnemailedReceipt : on entry — EmailReceipt inserts ReceiptEmail (fires after commit, in its own transaction, healed at every Hourly tick)
```

### ChangeDueDate

A transient act is an input, not state: its partitions are decided once, at the act's commit, and the instance is not kept — the final state is its removal at the envelope's close. Only its consequences persist.

```mermaid
stateDiagram-v2
    state decide <<choice>>
    [*] --> decide : commitChangeDueDate
    decide --> ApplicableDueChange : not (invoice is IssuedInvoice)
    ApplicableDueChange --> [*] : ApplyDueChange — sets invoice.due = newDue
    decide --> RefusedDueChange : invoice is IssuedInvoice
    RefusedDueChange --> [*] : RecordDueChangeRefusal — inserts DueChangeRefusal
```

The two guards are complements, so every ChangeDueDate takes exactly one branch — the structural proof that each request gets a decision.

## Rule graph

The whole system on one page: an edge means the source's commit can change the target rule's condition — **may**-cause, like everything else here. Solid edges fire inside the same transaction envelope; dotted edges cross a boundary (`after commit`, or armed now and swept at the labeled tick). Fan-out is not a choice: every edge whose guard holds fires, in no particular order. A diamond appears only where exclusivity is proven — the accept/refuse split of an input-constrained `never`, and a transient act's complement partitions. A self-loop marked *disarms its own guard* is the guard-disarm pattern: the rule's own write makes its re-evaluation harmless. An act with no outgoing edge commits quietly — data recorded, no rule woken.

```mermaid
flowchart LR
    commitCustomer["commitCustomer"]
    commitCorrectEmail["commitCorrectEmail"]
    commitInvoice["commitInvoice"]
    commitLineItem["commitLineItem"]
    commitPayment["commitPayment"]
    commitIssuance["commitIssuance"]
    commitChangeDueDate["commitChangeDueDate"]
    commitArchiveRequest["commitArchiveRequest"]
    commitUnarchiveRequest["commitUnarchiveRequest"]
    ApplyEmailCorrection["ApplyEmailCorrection"]
    TrackLargestPayment["TrackLargestPayment"]
    SendReceipt["SendReceipt"]
    EmailReceipt["EmailReceipt"]
    RemindOverdue["RemindOverdue"]
    ApplyDueChange["ApplyDueChange"]
    RecordDueChangeRefusal["RecordDueChangeRefusal"]
    NoteUnarchival["NoteUnarchival"]
    commitCorrectEmail --> ApplyEmailCorrection
    commitLineItem --> qLineItem{"quantity <= 0 or price < 0 ?"}
    qLineItem -->|"violated — refused, nothing begins"| refLineItem(["refusal"])
    qLineItem -->|"passes"| envLineItem["the LineItem envelope"]
    envLineItem -->|"when PaidInvoice"| SendReceipt
    envLineItem -.->|"swept every Weekly — when ActionableOverdue …"| RemindOverdue
    commitPayment --> qPayment{"amount <= 0 ?"}
    qPayment -->|"violated — refused, nothing begins"| refPayment(["refusal"])
    qPayment -->|"passes"| envPayment["the Payment envelope"]
    envPayment --> TrackLargestPayment
    envPayment -->|"when PaidInvoice"| SendReceipt
    commitChangeDueDate --> qChangeDueDate{"invoice is IssuedInvoice ?"}
    qChangeDueDate -->|"yes — exactly one branch, the guards are complements"| RecordDueChangeRefusal
    qChangeDueDate -->|"no"| ApplyDueChange
    commitUnarchiveRequest -.->|"swept every Weekly — when ActionableOverdue …"| RemindOverdue
    commitUnarchiveRequest -->|"when leaving ArchivedInvoice"| NoteUnarchival
    SendReceipt -.->|"inserts Receipt — after commit, healed every Hourly"| EmailReceipt
    EmailReceipt -.->|"inserts ReceiptEmail — disarms its own guard"| EmailReceipt
    RemindOverdue -.->|"inserts Reminder — disarms its own guard"| RemindOverdue
    ApplyDueChange -.->|"writes Invoice.due — swept every Weekly — when ActionableOverdue …"| RemindOverdue
```

- **RemindOverdue** fires when ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days))))

## Contention map

Where the system can work in parallel and where work must wait its turn. Work sharing a **queue key** is handled one item at a time in arrival order (U3), like any queue; work whose keys differ runs independently. A key is evaluated per call — `deposit.account` names the row that call's work queues on. Two envelopes contend iff their key sets intersect: path keys by the row they evaluate to, value keys by equal committed values, and a system-wide row with anything touching its shape. Derived from the spec's own read and write paths — no author declares a queue key.

| envelope | queues on | notes |
|---|---|---|
| commitCustomer | nothing — contends with no other work |  |
| commitCorrectEmail | `correctEmail.customer` | work with equal keys takes turns; different keys run at the same time |
| commitInvoice | `invoice.customer` | work with equal keys takes turns; different keys run at the same time |
| commitLineItem | `lineItem.invoice` | work with equal keys takes turns; different keys run at the same time |
| commitPayment | `payment.invoice`, `payment.invoice.customer` | work with equal keys takes turns; different keys run at the same time |
| commitIssuance | `issuance.invoice` | work with equal keys takes turns; different keys run at the same time |
| commitChangeDueDate | `changeDueDate.invoice`, `changeDueDate.invoice.customer` | work with equal keys takes turns; different keys run at the same time |
| commitArchiveRequest | `archiveRequest.invoice` | work with equal keys takes turns; different keys run at the same time |
| commitUnarchiveRequest | `unarchiveRequest.invoice` | work with equal keys takes turns; different keys run at the same time |
| EmailReceipt (each Hourly firing) | `this` | work with equal keys takes turns; different keys run at the same time |
| RemindOverdue (each Weekly firing) | `this` | work with equal keys takes turns; different keys run at the same time |

## Sequence diagrams

One diagram per exposed act. Each diagram is **may-fire**: a rule appears wherever the commit could affect its condition, with the condition shown as a note — whether it actually fires depends on the data at that commit. Frames (`critical`) are transaction envelopes: everything inside one frame stands or falls together.

### commitCustomer

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitCustomer(name, email)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the Customer envelope
        S->>S: insert Customer
        Note over S: no rules fire inside this envelope
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
```

### commitCorrectEmail

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitCorrectEmail(customer, corrected)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the CorrectEmail envelope
        S->>S: insert CorrectEmail
        Note over S: ApplyEmailCorrection — when CorrectEmail
        S->>S: customer.email = corrected
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
```

### commitInvoice

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitInvoice(customer, due)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the Invoice envelope
        S->>S: insert Invoice
        Note over S: SendReceipt — when PaidInvoice
        S->>S: insert Receipt
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    S-)S: after commit — EmailReceipt begins its own transaction (triggered by the Receipt created above)
    critical T2 — EmailReceipt firing
        Note over S: EmailReceipt — when UnemailedReceipt
        S->>S: insert ReceiptEmail
    end
    Note right of S: backstop — every Hourly tick re-checks UnemailedReceipt and heals anything the boundary dropped, the guard makes re-evaluation harmless
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

### commitLineItem

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitLineItem(invoice, description, price, quantity)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the LineItem envelope
        S->>S: insert LineItem
        Note over S: SendReceipt — when PaidInvoice
        S->>S: insert Receipt
        Note over S: transaction-end check — never LineItem where quantity <= 0
        Note over S: transaction-end check — never LineItem where price < 0
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    S-)S: after commit — EmailReceipt begins its own transaction (triggered by the Receipt created above)
    critical T2 — EmailReceipt firing
        Note over S: EmailReceipt — when UnemailedReceipt
        S->>S: insert ReceiptEmail
    end
    Note right of S: backstop — every Hourly tick re-checks UnemailedReceipt and heals anything the boundary dropped, the guard makes re-evaluation harmless
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

### commitPayment

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitPayment(invoice, amount)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the Payment envelope
        S->>S: insert Payment
        Note over S: TrackLargestPayment — when Payment
        S->>S: invoice.customer.largestPayment = max(invoice.customer.largestPayment, amount)
        Note over S: SendReceipt — when PaidInvoice
        S->>S: insert Receipt
        Note over S: transaction-end check — never Payment where amount <= 0
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    S-)S: after commit — EmailReceipt begins its own transaction (triggered by the Receipt created above)
    critical T2 — EmailReceipt firing
        Note over S: EmailReceipt — when UnemailedReceipt
        S->>S: insert ReceiptEmail
    end
    Note right of S: backstop — every Hourly tick re-checks UnemailedReceipt and heals anything the boundary dropped, the guard makes re-evaluation harmless
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

### commitIssuance

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitIssuance(invoice)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the Issuance envelope
        S->>S: insert Issuance
        Note over S: no rules fire inside this envelope
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
```

### commitChangeDueDate

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitChangeDueDate(invoice, newDue)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the ChangeDueDate envelope
        S->>S: insert ChangeDueDate
        Note over S: transient act — its partitions are decided once, at this commit, and the instance is removed at the envelope's close (only consequences persist)
        Note over S: ApplyDueChange — when ApplicableDueChange
        S->>S: invoice.due = newDue
        Note over S: RecordDueChangeRefusal — when RefusedDueChange
        S->>S: insert DueChangeRefusal
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

### commitArchiveRequest

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitArchiveRequest(invoice)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the ArchiveRequest envelope
        S->>S: insert ArchiveRequest
        Note over S: NoteUnarchival — when leaving ArchivedInvoice
        S->>S: insert UnarchiveNotice
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

### commitUnarchiveRequest

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitUnarchiveRequest(invoice)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the UnarchiveRequest envelope
        S->>S: insert UnarchiveRequest
        Note over S: NoteUnarchival — when leaving ArchivedInvoice
        S->>S: insert UnarchiveNotice
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```
