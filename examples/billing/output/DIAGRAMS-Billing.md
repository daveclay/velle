# Sequence diagrams — Billing

Generated from the spec — a deterministic projection, regenerated by `gradle generate`; do not edit. One diagram per exposed act. Each diagram is **may-fire**: a rule appears wherever the commit could affect its condition, with the condition shown as a note — whether it actually fires depends on the data at that commit. Frames (`critical`) are transaction envelopes: everything inside one frame stands or falls together.

## commitCustomer

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitCustomer(name, email)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the Customer envelope
        S->>S: insert Customer
        Note over S: no rules fire inside this envelope
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
```

## commitCorrectEmail

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitCorrectEmail(customer, corrected)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the CorrectEmail envelope
        S->>S: insert CorrectEmail
        Note over S: ApplyEmailCorrection — when CorrectEmail
        S->>S: customer.email = corrected
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
```

## commitInvoice

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitInvoice(customer, due)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the Invoice envelope
        S->>S: insert Invoice
        Note over S: SendReceipt — when PaidInvoice
        S->>S: insert Receipt
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    S-)S: after commit — EmailReceipt begins its own transaction (triggered by the Receipt created above)
    critical T2 — EmailReceipt firing
        Note over S: EmailReceipt — when UnemailedReceipt
        S->>S: insert ReceiptEmail
    end
    Note right of S: backstop — every Hourly tick re-checks UnemailedReceipt and heals anything the boundary dropped, the guard makes re-evaluation harmless
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

## commitLineItem

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitLineItem(invoice, description, price, quantity)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the LineItem envelope
        S->>S: insert LineItem
        Note over S: SendReceipt — when PaidInvoice
        S->>S: insert Receipt
        Note over S: transaction-end check — never LineItem where quantity <= 0
        Note over S: transaction-end check — never LineItem where price < 0
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    S-)S: after commit — EmailReceipt begins its own transaction (triggered by the Receipt created above)
    critical T2 — EmailReceipt firing
        Note over S: EmailReceipt — when UnemailedReceipt
        S->>S: insert ReceiptEmail
    end
    Note right of S: backstop — every Hourly tick re-checks UnemailedReceipt and heals anything the boundary dropped, the guard makes re-evaluation harmless
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

## commitPayment

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitPayment(invoice, amount)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the Payment envelope
        S->>S: insert Payment
        Note over S: TrackLargestPayment — when Payment
        S->>S: invoice.customer.largestPayment = max(invoice.customer.largestPayment, amount)
        Note over S: SendReceipt — when PaidInvoice
        S->>S: insert Receipt
        Note over S: transaction-end check — never Payment where amount <= 0
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    S-)S: after commit — EmailReceipt begins its own transaction (triggered by the Receipt created above)
    critical T2 — EmailReceipt firing
        Note over S: EmailReceipt — when UnemailedReceipt
        S->>S: insert ReceiptEmail
    end
    Note right of S: backstop — every Hourly tick re-checks UnemailedReceipt and heals anything the boundary dropped, the guard makes re-evaluation harmless
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

## commitIssuance

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitIssuance(invoice)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the Issuance envelope
        S->>S: insert Issuance
        Note over S: no rules fire inside this envelope
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
```

## commitChangeDueDate

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitChangeDueDate(invoice, newDue)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the ChangeDueDate envelope
        S->>S: insert ChangeDueDate
        Note over S: transient act — its partitions are decided once, at this commit, and the instance is removed at the envelope's close (only consequences persist)
        Note over S: ApplyDueChange — when ApplicableDueChange
        S->>S: invoice.due = newDue
        Note over S: RecordDueChangeRefusal — when RefusedDueChange
        S->>S: insert DueChangeRefusal
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

## commitArchiveRequest

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitArchiveRequest(invoice)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the ArchiveRequest envelope
        S->>S: insert ArchiveRequest
        Note over S: NoteUnarchival — when leaving ArchivedInvoice
        S->>S: insert UnarchiveNotice
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```

## commitUnarchiveRequest

```mermaid
sequenceDiagram
    actor Caller
    participant S as Billing
    Caller->>S: commitUnarchiveRequest(invoice)
    Note over Caller,S: boundary validation first — type checks and the input-constrained nevers compiled to this expose site, a violation is a refusal and nothing begins
    critical T1 — the UnarchiveRequest envelope
        S->>S: insert UnarchiveRequest
        Note over S: NoteUnarchival — when leaving ArchivedInvoice
        S->>S: insert UnarchiveNotice
    end
    S-->>Caller: accepted — or an error anywhere in the frame rolled the whole envelope back
    Note right of S: at each Weekly tick — RemindOverdue sweeps current members of ActionableOverdue where not (exists (Reminder where (invoice == this) and (sentOn > (today - 7 days)))) in its own transaction per record
```
