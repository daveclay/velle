// GENERATED by Velle from the spec — do not edit. Regenerate with: gradle generate
package velle.generated.orders.specs

import kotlin.test.*
import velle.*
import velle.generated.*
import velle.generated.orders.*

/**
 * rule ReserveStock when OrderLine {
 *     Reservation from { line: this, quantity: this.quantity }
 * }
 *
 * never (OrderLine where quantity <= 0)
 *
 */
class OrderLineSpec : SpecSupport() {

    @Test
    fun `ReserveStock - a new OrderLine produces a Reservation`() {
        val beforeReservation = count("Reservation")
        // given: one new subject entered 'OrderLine'
        val orderLine = givens.orderLine()
        orderLine.assertIsA("OrderLine", "the given must deliver a member of 'OrderLine'")
        assertEquals(beforeReservation + 1, count("Reservation"), "rule ReserveStock: one 'Reservation' per firing")
        val producedReservation = last("Reservation")
        assertEquals(orderLine.id, field(producedReservation, "line"), "Reservation.line: this")
    }
}
