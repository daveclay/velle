// GENERATED by Velle from the spec — do not edit. Regenerate with: gradle generate
package velle.generated.partitiondrift

import velle.generated.PartitionDriftSystem

/**
 * The scenarios the generated specs require — the human-owned half of the
 * given/derived split (testgen.md). Implement as:
 *
 *     package velle.generated.partitiondrift
 *     class Givens(private val sys: PartitionDriftSystem) : RequiredGivens { ... }
 *
 * A missing given is a compile error naming exactly what the spec's tests are owed.
 */
interface RequiredGivens {

    /** Commit ONE new 'CreateNote' that enters 'CreateNote' at its commit. The act is transient — nothing to return. */
    fun createNote()

    /** Perform the commit(s) that make ONE new subject enter 'ApplicableBareEdit'; return the subject. */
    fun applicableBareEdit(): PartitionDriftSystem.BareEditView

    /** Perform the commit(s) that make ONE new subject enter 'RefusedBareEdit'; return the subject. */
    fun refusedBareEdit(): PartitionDriftSystem.BareEditView

    /** Perform the commit(s) that make ONE new subject enter 'ApplicableSafeEdit'; return the subject. */
    fun applicableSafeEdit(): PartitionDriftSystem.SafeEditView

    /** Perform the commit(s) that make ONE new subject enter 'RefusedSafeEdit'; return the subject. */
    fun refusedSafeEdit(): PartitionDriftSystem.SafeEditView

    /** Commit ONE new 'TransientEdit' that enters 'ApplicableTransientEdit' at its commit. The act is transient — nothing to return. */
    fun applicableTransientEdit()

    /** Commit ONE new 'TransientEdit' that enters 'RefusedTransientEdit' at its commit. The act is transient — nothing to return. */
    fun refusedTransientEdit()

    /** Perform the commit(s) that make ONE new subject enter 'LockNote'; return the subject. */
    fun lockNote(): PartitionDriftSystem.LockNoteView

    /** Perform the commit(s) that make ONE new subject enter 'UnlockNote'; return the subject. */
    fun unlockNote(): PartitionDriftSystem.UnlockNoteView

    /** Perform the commit(s) that make ONE new subject enter 'RenameText'; return the subject. */
    fun renameText(): PartitionDriftSystem.RenameTextView
}
