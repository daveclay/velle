// GENERATED by Velle from the spec — do not edit. Regenerate with: gradle generate
package velle.generated

/**
 * The scenarios the generated specs require — the human-owned half of the
 * given/derived split (testgen.md). Implement as:
 *
 *     package velle.generated
 *     class Givens(private val sys: BillingSystem) : RequiredGivens { ... }
 *
 * A missing given is a compile error naming exactly what the spec's tests are owed.
 */
interface RequiredGivens {

    /** Perform the commit(s) that make ONE new subject enter 'CorrectEmail' (fires rule ApplyEmailCorrection); return the subject's id. */
    fun enterApplyEmailCorrection(): Long

    /** Perform the commit(s) that make ONE new subject enter 'Payment' (fires rule TrackLargestPayment); return the subject's id. */
    fun enterTrackLargestPayment(): Long

    /** Provide any committed 'Invoice'; return its id. */
    fun someInvoice(): Long

    /** Perform the commit(s) that make ONE new subject enter 'PaidInvoice' (fires rule SendReceipt); return the subject's id. */
    fun enterSendReceipt(): Long

    /** Perform the commit(s) that make ONE new subject enter 'UnemailedReceipt' (rule EmailReceipt then fires after the transaction); return the trigger subject's id. */
    fun enterEmailReceipt(): Long

    /** Bring ONE subject into 'ActionableOverdue' without ticking 'Weekly'; return the subject's id. */
    fun populateRemindOverdue(): Long

    /** Perform the commit(s) that make ONE new subject enter 'ApplicableDueChange' (fires rule ApplyDueChange); return the subject's id. */
    fun enterApplyDueChange(): Long

    /** Perform the commit(s) that make ONE new subject enter 'RefusedDueChange' (fires rule RecordDueChangeRefusal); return the subject's id. */
    fun enterRecordDueChangeRefusal(): Long

    /** Create a member of 'ArchivedInvoice', then perform the commit that makes it leave (fires rule NoteUnarchival); return the subject's id. */
    fun exitNoteUnarchival(): Long
}
