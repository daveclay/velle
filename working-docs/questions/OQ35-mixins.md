# OQ35 — Cross-shape structural mixins (post-v0)

**Status:** deferred — post-v0; README §22 is missing its item (restore it)
**In plain terms:** can unrelated shapes share a definition like "overdue" without pretending to share a hierarchy?

---

README §9 points at "Open/unresolved" but §22 has no such item; restore the item there — or take on the design: a trait like `Overdue` reusable across unrelated shapes.
