# Open questions — the index

One line per question here; the discussion lives in `questions/OQ<n>-*.md`, where each file's header states the question in plain terms before the dense part begins. OQ tags are stable — numbers are never renumbered or reused, so references like `OQ16` stay valid forever, and gaps in the sequence are questions already settled (table at the bottom).

**When a question gets a tag:** any open design question with discussion worth keeping, or referenced from more than one doc, gets a tag and a file *at birth* — wherever it surfaced (investigation residue, a break-doc gap, a TODO parenthetical). A question that fits whole in one TODO line stays a TODO line.

**Lifecycle:** opened (tag + file) → discussion accumulates in the file → settled → the result promotes to the README or normative docs, the file is deleted, and the entry moves to the settled table with a pointer to where it retired.

The current milestone is calibrating v0 against realistic specs. Nothing here blocks the build; OQ16 — which since OQ15's settling also carries the V14 certificate-whitelist calibration — is precisely the question v0 exists to answer empirically.

## Open

- **[OQ16](questions/OQ16-order-independence.md)** — can the compiler prove sibling firing order never matters? · calibration; completeness audit done 2026-08-20 (`audit-sibling-confluence.md`: F1–F5 fixed, payments C1 resolved spec-side, Newman argument + residue); also carries the V14 certificate-whitelist calibration (OQ15's residue, one shared vocabulary)
- **[OQ17](questions/OQ17-rejection-scope.md)** — rejection scope: what unwinds, what is the caller told, can refusal be partial? · minimal v0 answer settled
- **[OQ27](questions/OQ27-erasure.md)** — erasure and retention (right-to-be-forgotten) · language-side resolved via OQ37-R10 (`? initially required` + ordinary `= none` rules); residue: compilation's unretrievability guarantee
- **[OQ28](questions/OQ28-bootstrap-backfill.md)** — bootstrap/backfill: a new rule against pre-existing state · not designed
- **[OQ29](questions/OQ29-sum-types.md)** — sum types / union shapes · workaround exists (two optionals + xor `never`)
- **[OQ37](questions/OQ37-delete.md)** — delete as a described mutation: statement, existence-dependency check, gating, referential completeness · investigation open (`investigate-delete.md`)
- **[OQ39](questions/OQ39-inline-part-creation.md)** — inline part creation: multi-part acts through the generated commit function · direction settled (one commit = one statically-shaped closure; author states closure membership; v0 trees only; creation-only — updates never ride it; part-triggered container writes refused totally, derive-or-container-granularity), syntax not designed; transient containers spun off as OQ43
- **[OQ43](questions/OQ43-transient-containers.md)** — transient containers: a closure whose container is `expose transient` · spun off from OQ39 (2026-08-21); V17's direction forces the all-transient tree — remaining: the scoped V17 carve-out (same-closure references, `with`-resolution-only inverse inference), V18's reach over transient parts, the durable-container/transient-branch mix

## Deferred — post-v0 re-derivations (README §22's list)

- **[OQ32](questions/OQ32-requires.md)** — `requires`, re-derived
- **[OQ33](questions/OQ33-visible-to.md)** — `visible to` field visibility, re-derived
- **[OQ34](questions/OQ34-schedule-definition.md)** — schedule definition (carries event-anchored timeouts, data-derived durations)
- **[OQ35](questions/OQ35-mixins.md)** — cross-shape structural mixins (§22 item missing; restore it)

## Settled

| tag | retired to |
|---|---|
| OQ1–4, 6, 8–13, 18 | settled before this index existed; results live in the README |
| OQ5 — external input | README §22 "External input (`expose`)" |
| OQ7 — exit reads | README §13; two residual threads became §22 items (latency vocabulary; `on commit of` narrowing) |
| OQ14 — guard adoption | resolved by author judgment: the canonical guard form (README §18) stands as what fold diagnostics (§19) demand — no sugar added |
| OQ19 — boundary/apparatus legibility | became a calibration rider on the spec-writing item (TODO.md) |
| OQ36 — the universal-transaction contract | `evaluation.md`, "The universal transaction" (U1–U5: snapshot, atomicity, serialization, permanence, no side doors — precise guarantee + in-practice prose per clause, the proof-spends table, the exclusions); pointers in README §11/§22; glossary updated. U3's serialization-domain refinement (the derived queue key) spun off as OQ40 |
| OQ30 — author-named `many` fields: the commit story | README §6 (ownership frame; "Committing and assigning collections"; "Renamed and derived collections"), `grammar.md` (propType, `empty`, setExpr, assignment note, operator line), `checks.md` (V19–V20; F2/F4/V1 extensions), `evaluation.md` (stored edge sets; boundary duplicate refusal). Residue rulings OQ30-R1–R6 promoted with it; R5 spun off OQ38 (bags), R7 spun off OQ39 (inline part creation) |
| OQ20 — commit-refusal | not a primitive: refusal is compiled boundary code from `never` (README §21); the who-may-commit residue retired to engineer wrapper code (README §22; `investigate_runtime.md` §1) |
| OQ21 — construct set · OQ24 — harness boundary | README §22's scope statement |
| OQ22 — grammar | `grammar.md` (normative) |
| OQ23 — builtin surface | README §5, §10 |
| OQ25 — operational semantics & check catalog | `evaluation.md`, `checks.md` (normative) |
| OQ40 — serialization domains (the derived queue key) | `evaluation.md` U3 (the derivation, conflict-iff-domains-intersect, the surfaces, the cross-store exclusion); `checks.md` A5 (the width advisory — ruled advisory 2026-08-18, cadence discharge, dead tolerance); `grammar.md` (`tolerates contention` on rules and nevers); `diagrams.md` (the contention map, built); glossary updated. Implementation: `Domains.kt`, `ContentionMapGen.kt`, the queue-key contract in `Codegen.kt`, `DomainsTest`/`CommutationTest`. Trust residue (soundness audit, property sweep, the advisory dial) spun off as OQ42 |
| OQ41 — the compilation-artifact family | `diagrams.md` (normative): the projection principle, the four built members (class, state-flow, rule graph, sequence — `--diagrams`, billing's `DIAGRAMS-Billing.md`) with their proof-backed mappings, the tier ladder, notation (Mermaid default, PlantUML designated opt-in); README pointers in the opening and §22's architecture; glossary "Generated artifacts" section. Residue rides existing homes: tier 2 on `example` (`testgen.md` phase 3), tier 3 on provenance (§22), the contention map settled with OQ40 (built), the timing-diagram kind on the episode machinery |
| OQ26 — correlation keys for transient acts | resolved by author judgment (2026-08-19): the language offers nothing beyond what already exists — it trusts the client's key mechanism and the author's spelling, because anything more (storing, generating, or uniqueness-checking client keys) sits below the abstraction layer velle occupies. The convention promoted as the documented idiom in README §4 "Transient acts" ("Correlating responses to requests"); glossary updated. The four undesigned items close with it: the threading ceremony is the author's visible idempotency decision (the "no guard sugar" trade); uniqueness is the client's to own; in-flight duplicates are two serialized transactions met by whatever guard the author wrote; and the mechanism stays informal — no new language surface |
| OQ42 — trusting the domain derivation | `working-docs/audit-symmetric-evaluation.md` (the 13-case symmetric-evaluation matrix, every verdict pinned by `SymmetricEvaluationAuditTest` probes; F1 — a reassignment's incoming reference went unkeyed for transient/ambiguous-inverse acts — fixed in `walkAssignment`, membership/enrollment contracts gained the keys; R1 ruled 2026-08-19: captures are committed state readable at admission → `evaluation.md` U3's admission-moment clause, captures included). Body-side `correlatable` (`bodyCorrelationFields`, 2026-08-18). The generated commutation sweep (`CommutationSweepTest` over every example spec; `DomainKeys.kt` as the queue-key contract's reference implementation). The A5 dial ruled advisory, reaffirmed → `docs/concurrency.md` (the two-authors record). Residue to TODO: sweep extensions (randomized values, tick pairs, triples) and audit precision P2–P4; the A5 stress test rides OQ16 |
| OQ31 — committer-suppliable fields | ruled 2026-08-20: there is no field policy — which fields the committer supplies is read off the declaration forms. Promoted to README §22 "External input (`expose`)": the generated commit function takes every stored field of the exposed shape as a required parameter (convenience defaults live in the engineer's wrapper); derived properties are legal and never parameters (nothing stored to supply — on a transient act, the once-evaluated arrival-time value); `initially`/`timestamp` on an exposed shape is a compile error (`checks.md` V21) — system-maintained values live on unexposed shapes, supplied by a materializing rule's `from` block or generated at the record's creation, with §4's transient materialization as the served spelling. Examples migrated to the split 2026-08-20; implementation residue (V21 in the validator, `commitFn` cleanup, SpecGen coverage for rule-materialized records) → TODO.md |
| OQ38 — duplicates in a relationship: a multiplicity-bearing `many`? | ruled 2026-08-20: no — collections stay sets, with no bag modifier. A relationship that genuinely carries multiplicity is modeled as data on an edge shape (a quantity, a role, a time) — the graduation point the boundary refusal's diagnostic already names. README §6 ("Committing and assigning collections") states the decided semantics; nothing new to promote |
| OQ15 — ordered folds and firing order at a tick | ruled 2026-08-19: rules carry no ordering clause — firing order is never language surface (an ordering clause would contradict OQ16's never-specified-order direction, manage at execution time what structure makes impossible, and restate an intent that already lives in the data model as the recurrence's ordering datum). The **predecessor recurrence** is the served spelling, promoted to README §19 ("Order-dependent folds at a tick") with the worked streak example, the late-arrival argument (a stored fold describes arrival order and keeps a wrong value; the recurrence re-derives to the truth), and the well-foundedness check (`checks.md` V14: stratify, then certify — certificate acceptance implemented in `Validator.kt` alongside the settling). Commit-cadence for the mutation form is arrival-order semantics, the permanent answer. Ties are model-side; `id` can never tiebreak (README §5, §10). Residue — growing the V14 certificate whitelist — rides OQ16 as one shared certificate vocabulary (V14 + V16). Glossary gained *predecessor recurrence*, *arrival-order semantics*, *certificate* |
